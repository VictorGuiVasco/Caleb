import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    height: 50,
    marginBottom: 10,
    marginHorizontal: 20,
    

    justifyContent: 'center',

    borderBottomWidth: 1,
    borderBottomColor: '#DAE1F7'
  },

  title: {
    fontFamily: 'Rubik_700Bold',
    fontSize: 26,
    color: '#000',
    lineHeight: 30
  }
})

export default styles